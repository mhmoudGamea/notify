import 'package:flutter/material.dart';

abstract class AppColors {
  static const primaryColor = Colors.blue;
  static final lightPrimaryColor = Colors.blue.withOpacity(0.5);
  static const successColor = Color(0xCC87CEFA);
  static const failColor = Color(0xCCFA8787);
}
